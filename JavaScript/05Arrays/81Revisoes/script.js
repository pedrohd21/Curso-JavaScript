// const nomes = ['eduardo', 'maria', 'joana']

// delete nomes[2] // apaga o dado da lista
// nomes.pop() // apaga o ultimo
// nomes.shift() // renove o primeiro
// const novo = [...nomes] // copia o array, sempre fazer assim
// console.log(nomes.length) // tamanho do array
// nomes.push('Adiciona no final') // adiciona mais no array
// nomes.unshift('Adiciona na frente') // add no unicio array 
// const novo = nomes.slice(1, 3) // vai mostrar o array ate um determinado 

const nome = 'pedro henrique de papp'
const nomes = nome.split(' ') // separa a coisa pelo espaco, pode ser virgula tbm
console.log(nomes)

const nome1 = ['pedro', 'henrique', 'de', 'papp']
const nome2 = nome1.join(' ') // junta um array
console.log(nome2)